/** Default seed used to make generated datasets repeatable. */
export const DEFAULT_DATA_SEED = 42;

/** Number of records generated before yielding to the browser event loop. */
export const DATA_GENERATION_CHUNK_SIZE = 25_000;
